-- Sleeper's Tomb 1.0 -- zone init for instance version 1 only.
-- Replaces the base script_init.lua in a v1 expedition, so the 2.0 encounters
-- (motg + sleeper_custom) do NOT load in this instance.
eq.load_encounter("Sleeper");
